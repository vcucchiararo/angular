(function () {
  let message: string = "Hola";
  let number: number = 123;
  let boolean: boolean = true; // false
  let today: Date = new Date();

  let anything;
  anything = message;
  anything = number;
  anything = boolean;
  anything = today;

  let spiderman = {
    name: "Peter",
    age: 30,
  };

  spiderman = {
    name: "Juan",
    age: 20,
  };
})();
